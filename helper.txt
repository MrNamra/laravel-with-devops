docker-compose down -v

docker-compose up --build -d

docker run --rm --network laravel-microservices_laravel_network \
  edenhill/kcat:1.7.1 \
  -b kafka:9092 \
  -t order.events \
  -C \
  -o beginning \
  -q